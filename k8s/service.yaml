apiVersion: v1
kind: Service
metadata:
  name: react-demo
  namespace: default
  labels:
    app: react-demo
spec:
  type: NodePort
  selector:
    app: react-demo
  ports:
    - port: 80
      targetPort: 80
      protocol: TCP
